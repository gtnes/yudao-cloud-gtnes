<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.infra.dal.mysql.softwarerecord.SoftwareRecordMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectByFingerprintAndIp" resultType="cn.iocoder.yudao.module.infra.dal.dataobject.softwarerecord.SoftwareRecordDO">
        SELECT * FROM infra_gtnes_software_record
        WHERE device_fingerprint = #{deviceFingerprint} AND ip = #{ip} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByFingerprint" resultType="cn.iocoder.yudao.module.infra.dal.dataobject.softwarerecord.SoftwareRecordDO">
        SELECT * FROM infra_gtnes_software_record
        WHERE device_fingerprint = #{deviceFingerprint} AND deleted = 0
        LIMIT 1
    </select>

    <!-- 查询今日新增合计：统计创建日期为今天的数据条数 -->
    <select id="selectTodayNewCount" resultType="Long">
        SELECT COUNT(*) FROM infra_gtnes_software_record
        WHERE app_type = #{appType} AND deleted = 0
        AND DATE(create_time) = CURDATE()
    </select>

    <!-- 查询今日更新合计：统计更新日期为今天的数据条数 -->
    <select id="selectTodayUpdateCount" resultType="Long">
        SELECT COUNT(*) FROM infra_gtnes_software_record
        WHERE app_type = #{appType} AND deleted = 0
        AND DATE(update_time) = CURDATE()
    </select>

    <!-- 查询数据总数：统计没有被删除的数据条数 -->
    <select id="selectTotalCount" resultType="Long">
        SELECT COUNT(*) FROM infra_gtnes_software_record
        WHERE app_type = #{appType} AND deleted = 0
    </select>

    <!-- 查询Windows平台总数：统计软件平台为Windows的数据条数（platform=1） -->
    <select id="selectWindowsCount" resultType="Long">
        SELECT COUNT(*) FROM infra_gtnes_software_record
        WHERE app_type = #{appType} AND platform = 1 AND deleted = 0
    </select>

    <!-- 查询Linux平台总数：统计软件平台为Linux的数据条数（platform=5） -->
    <select id="selectLinuxCount" resultType="Long">
        SELECT COUNT(*) FROM infra_gtnes_software_record
        WHERE app_type = #{appType} AND platform = 5 AND deleted = 0
    </select>

    <!-- 查询MacOS平台总数：统计软件平台为MacOS的数据条数（platform=3） -->
    <select id="selectMacOSCount" resultType="Long">
        SELECT COUNT(*) FROM infra_gtnes_software_record
        WHERE app_type = #{appType} AND platform = 3 AND deleted = 0
    </select>

</mapper>